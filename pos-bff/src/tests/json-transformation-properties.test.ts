afterAll(async () => {
  await new Promise<void>((resolve) => setTimeout(() => resolve(), 500));
});

describe("reading json transformation from properties", () => {
  describe("transformJson", () => {
    it("transform json privided mapper", async () => {
      // const details = await import("../config/");
    });
  });
});
