1. Use following command to build the docker file.
    docker-compose build

2. Use following command to run the docker build.
    docker-compose up

3. Health check is added for server at. 
    ${BASE_URL}/serverHealth

